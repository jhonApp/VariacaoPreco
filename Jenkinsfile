pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Iniciando a pipeline'
            }
        }
        stage('Test') {
            steps {
                echo 'Iniciando os testes'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Iniciando o deploy'
            }
        }
    }
}
